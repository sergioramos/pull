var git = require('../main');
var fs = require('fs');
var path = require('path');

git.debug('development');


git.bsync('git@github.com:ramitos/blog.git', './', ['master'], '', function(e, results) {
	//console.log(results);
});